//update

if (state == PS_LAND or state == PS_RESPAWN or state == PS_PRATLAND or state == PS_LANDING_LAG){
with (asset_get("oPlayer")){
DownB_count = 0;
  }
}

if (state == PS_LAND or state == PS_RESPAWN or state == PS_PRATLAND or state == PS_LANDING_LAG){
with (asset_get("oPlayer")){
SideB_count = 0;
  }
}